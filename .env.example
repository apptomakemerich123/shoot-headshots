FAL_KEY="key_id:key_secret"

STRIPE_SECRET_KEY="sk_test_..."
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY="pk_test_..."

# Resend (email delivery) — https://resend.com
RESEND_API_KEY="re_..."
# Optional; default is Resend onboarding sender for testing
# RESEND_FROM_EMAIL="Shoot <onboarding@resend.dev>"

# Vercel KV (production). If unset, the app uses .data/portr-store.json on disk.
# Opt in to in-memory only: PORT_MEMORY_STORE_ONLY=1
